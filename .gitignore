*.retry
*.log
.ansible/
.vault_pass

# Secrets and credentials
*.env
.env*
secrets.yml
vault.yml
*.pem
*.key
id_rsa*
# host_vars/ and group_vars/ tracked in this fork (public keys only, no secrets)
# host_vars/
# group_vars/
